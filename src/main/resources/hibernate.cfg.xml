<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/robus</property>
        <property name="connection.username">root</property>
        <property name="connection.password"/>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL5InnoDBDialect</property>

        <!-- Disable the second-level cache  -->
        <!--<property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>   -->

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class="org.reichhold.robus.jobs.Token" />
        <mapping class="org.reichhold.robus.jobs.JobAd" />
        <mapping class="org.reichhold.robus.jobs.CleanJobAd" />
        <mapping class="org.reichhold.robus.roles.Role" />
        <mapping class="org.reichhold.robus.roles.RoleTerm" />
        <mapping class="org.reichhold.robus.citeUlike.CulUser" />
        <mapping class="org.reichhold.robus.citeUlike.CulDocument" />
        <mapping class="org.reichhold.robus.citeUlike.CulTag" />
        <mapping class="org.reichhold.robus.citeUlike.CulAssignment" />

    </session-factory>

</hibernate-configuration>